import { Button } from "react-bootstrap";

export default function AdminDashboard() {
    return (
        <div>
            <h1>Admin Dashboard</h1>
            <Button variant="primary" href="dashboard/crudproduct">Primary</Button>
        </div>
    );
}